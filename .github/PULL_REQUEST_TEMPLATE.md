## What does this PR do?

<!-- One or two sentences. Link the issue: Fixes #123 -->

## How was it tested?

- [ ] `cargo test --workspace` passes locally
- [ ] `cargo fmt --all` and `cargo clippy --workspace --all-targets` are clean
- [ ] Behavior verified in the app (`fixtures/make-sample.sh` + `cargo run -p sluice -- open …`) if UI/git behavior changed

## Notes for the reviewer

<!-- Anything non-obvious: trade-offs, platform quirks, follow-ups. -->
